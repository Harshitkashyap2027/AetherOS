# Protocols

Shared contracts for desktop, android companion, backend, and cloud communication.

## Starter contracts
- `contracts/aether_envelope.proto` - base transport envelope and heartbeat message shape
- `contracts/device_state.schema.json` - device state JSON contract for API and sync workflows
