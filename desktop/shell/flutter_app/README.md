# Flutter Desktop Shell Starter

This is the executable starter app for the AetherOS desktop shell.

## Prerequisites
- Flutter SDK with desktop support enabled

## Run
```bash
cd /home/runner/work/AetherOS/AetherOS/desktop/shell/flutter_app
flutter pub get
flutter run -d windows
# or: flutter run -d linux
```
